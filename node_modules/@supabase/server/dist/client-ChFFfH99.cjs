const require_errors = require("./errors-BxfYUd-f.cjs");
const require_error_response = require("./error-response-Cwri_qj2.cjs");
const require_extract_credentials = require("./extract-credentials-DvhCCwzp.cjs");
const require_read_upstream_auth = require("./read-upstream-auth-yQlJO74r.cjs");
const require_create_context_client = require("./create-context-client-Bmmbug2X.cjs");
let _supabase_middleware = require("@supabase/middleware");
//#region src/core/parts/construction-failure.ts
const constructionFailure = Symbol.for("@supabase/server:constructionFailure");
/**
* Marks an error thrown while a Supabase client is constructed for the
* request. `withSupabase`'s boundary maps only marked errors to a JSON
* response; any other throw escaping a part or the handler propagates.
*
* The mark is a non-enumerable symbol property, so the error's class, own
* properties and `toJSON` payload are unchanged.
*/
function markConstructionFailure(error) {
	Object.defineProperty(error, constructionFailure, { value: true });
	return error;
}
/** True for an `EnvError` or `AuthError` carrying the construction mark. */
function isConstructionFailure(error) {
	return (error instanceof require_errors.EnvError || error instanceof require_errors.AuthError) && constructionFailure in error;
}
/**
* The JSON response for a construction failure: the error's own code, status,
* hint, details and docs, rendered exactly as `errorResponse` renders any
* other {@link SupabaseServerError}.
*/
function constructionFailureResponse(error, errors) {
	return require_error_response.errorResponse(error, { errors });
}
//#endregion
//#region src/middleware/client/index.ts
const base = (0, _supabase_middleware.defineMiddleware)({
	key: "supabase",
	run: (config) => async (req, ctx) => {
		const upstream = require_read_upstream_auth.readUpstreamAuth(ctx);
		const { token: bearer } = require_extract_credentials.extractCredentials(req);
		const rawToken = bearer && !bearer.startsWith("sb_") ? bearer : void 0;
		const token = upstream.authMode === void 0 || upstream.authMode === "user" ? rawToken : void 0;
		const keyName = upstream.authMode === "publishable" ? upstream.authKeyName : void 0;
		let supabase;
		try {
			supabase = require_create_context_client.createContextClient({
				auth: {
					token,
					keyName
				},
				env: config?.env,
				supabaseOptions: config?.supabaseOptions
			});
		} catch (e) {
			throw markConstructionFailure(e instanceof require_errors.EnvError ? e : require_errors.Errors[require_errors.CreateSupabaseClientError]({ cause: e }));
		}
		return { supabase };
	}
});
/**
* **Alpha.** Contributes `ctx.supabase` — a Supabase client scoped to the
* caller's identity, so Row-Level Security policies apply. This is the same
* middleware `withSupabase` composes internally to build its context.
*
* Standalone, the caller's Bearer token (when present) is attached unverified —
* PostgREST verifies it on every query. Compose {@link middleware/claims!withClaims}
* upstream when the pipeline itself needs verified claims.
*
* @throws {@link index.EnvError} When `SUPABASE_URL` or the publishable key is
* missing — composing wrappers (like `withSupabase`) map this to a 500
* response; standalone pipelines see it as a thrown error.
*
* @example Standalone pipeline
* ```ts
* import { pipeline } from '@supabase/middleware'
* import { withSupabaseClient } from '@supabase/server/middleware/client'
*
* export default {
*   fetch: pipeline([withSupabaseClient()], async (req, ctx) => {
*     const { data } = await ctx.supabase.from('posts').select('id, title')
*     return Response.json(data)
*   }),
* }
* ```
*
* The composable middleware surface tracks `@supabase/middleware` 0.x — entry
* shapes, context keys, and config options may change between 0.x releases.
*
* @alpha
* @category Middleware
*/
function withSupabaseClient(config) {
	return base(config);
}
//#endregion
Object.defineProperty(exports, "constructionFailureResponse", {
	enumerable: true,
	get: function() {
		return constructionFailureResponse;
	}
});
Object.defineProperty(exports, "isConstructionFailure", {
	enumerable: true,
	get: function() {
		return isConstructionFailure;
	}
});
Object.defineProperty(exports, "withSupabaseClient", {
	enumerable: true,
	get: function() {
		return withSupabaseClient;
	}
});
